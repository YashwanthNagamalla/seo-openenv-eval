[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "seo-openenv"
version = "0.1.0"
description = "SEO OpenEnv for Scaler assessment"
readme = "README.md"
requires-python = ">=3.8"
authors = ["Yashwanth <you@example.com>"]
